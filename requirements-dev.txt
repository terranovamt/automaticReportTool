# ============================================================================
# ART.stdf - Development Dependencies
# ============================================================================
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# ============================================================================

# Include production dependencies
-r requirements.txt

# ----------------------------------------------------------------------------
# Testing Frameworks
# ----------------------------------------------------------------------------
pytest>=7.4.0,<8.0.0           # Testing framework
pytest-cov>=4.1.0,<5.0.0       # Code coverage for pytest
pytest-xdist>=3.3.0,<4.0.0     # Parallel test execution
pytest-mock>=3.11.0,<4.0.0     # Mocking support for pytest

# ----------------------------------------------------------------------------
# Code Quality and Linting
# ----------------------------------------------------------------------------
ruff>=0.1.0,<1.0.0             # Fast Python linter (replaces flake8, isort, etc.)
black>=23.0.0,<24.0.0          # Code formatter
mypy>=1.5.0,<2.0.0             # Static type checker
pylint>=3.0.0,<4.0.0           # Comprehensive code analyzer

# ----------------------------------------------------------------------------
# Documentation
# ----------------------------------------------------------------------------
sphinx>=7.2.0,<8.0.0           # Documentation generator
sphinx-rtd-theme>=1.3.0,<2.0.0 # Read the Docs theme
myst-parser>=2.0.0,<3.0.0      # Markdown support for Sphinx

# ----------------------------------------------------------------------------
# Development Tools
# ----------------------------------------------------------------------------
ipython>=8.12.0,<9.0.0         # Enhanced interactive Python shell
jupyter>=1.0.0,<2.0.0          # Jupyter notebook support
ipdb>=0.13.13,<1.0.0           # IPython debugger

# ----------------------------------------------------------------------------
# Performance Profiling
# ----------------------------------------------------------------------------
memory-profiler>=0.61.0,<1.0.0 # Memory usage profiling
line-profiler>=4.1.0,<5.0.0    # Line-by-line profiling

# ----------------------------------------------------------------------------
# Build and Packaging
# ----------------------------------------------------------------------------
build>=1.0.0,<2.0.0            # Build tool for Python packages
twine>=4.0.0,<5.0.0            # Package upload tool for PyPI

# ============================================================================
# Installation Instructions
# ============================================================================
# 1. Install all dependencies (production + development):
#    pip install -r requirements.txt -r requirements-dev.txt
#
# 2. Setup pre-commit hooks (optional):
#    pre-commit install
#
# 3. Run tests:
#    pytest tests/ -v --cov=src
#
# 4. Format code:
#    black src/ tests/
#    ruff check src/ tests/ --fix
#
# 5. Type checking:
#    mypy src/
#
# 6. Generate documentation:
#    cd docs && sphinx-build -b html . _build/html
# ============================================================================
